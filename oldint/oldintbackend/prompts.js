import express from 'express';
const router = express.Router();

// Placeholder for prompts routes
router.get('/', (req, res) => {
  res.json({ message: 'Prompts endpoint' });
});

export default router; 