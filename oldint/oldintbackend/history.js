import express from 'express';
const router = express.Router();

// Placeholder for history routes
router.get('/', (req, res) => {
  res.json({ message: 'History endpoint' });
});

export default router; 